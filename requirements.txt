# ── Core (always required) ────────────────────────────────────────────────────
pydantic>=2.7,<3
python-dotenv>=1.0
tomli>=2.0; python_version < "3.11"
pyyaml>=6.0
openai>=1.30
anthropic>=0.28
aiohttp>=3.9

# ── Optional: install extras as needed ────────────────────────────────────────
# pip install manusclaw[browser]  or  pip install -e ".[browser]"
#
# browser  = playwright>=1.44  crawl4ai>=0.3
# search   = duckduckgo-search>=6.2
# server   = fastapi>=0.111  uvicorn[standard]>=0.30
# cli      = rich>=13.0  prompt_toolkit>=3.0
# cron     = croniter>=2.0
# ollama   = ollama>=0.2.0
# mistral  = mistralai>=1.0
# bedrock  = boto3>=1.34
# google   = google-generativeai>=0.7
# viz      = matplotlib>=3.9  mpld3>=0.5.10
#
# all      = everything above

# ── Dev / test ────────────────────────────────────────────────────────────────
pytest>=8
pytest-asyncio>=0.23
pytest-xdist>=3.5
